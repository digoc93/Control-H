var autoIncrement = require('mongoose-auto-increment');
var mongoose = require('mongoose'),
    Schema = mongoose.Schema,
    ObjectId = mongoose,ObjectId;

var connection = mongoose.createConnection("mongodb://localhost/ControlH3");
    autoIncrement.initilize(connection);

var taskSchema= new Schema({
    _id : {type: Number, index: {unique: false}},
    task : {type: String, required: true},
    details :{type : String},
    priority: {type : String, required: true, enum: ['Low','Mid','High']},
    //It's not required , because the responsible is assigned after
    responsible : {type: Number}
    comments : {type: String},
    remainingTime: {type: Number, required: true}
});

taskSchema.plugin(connection.plugin, 'task');
module.exports = mongoose.model('task', taskSchema);