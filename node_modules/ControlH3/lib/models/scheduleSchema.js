var schema = require('./entities/scheduleStructure.js').data;
var autoIncrement = require('mongoose-auto-increment');
var mongoose = require('mongoose'),
	Schema = mongoose.Schema,
	ObjectId = Schema.ObjectId;

var connection = mongoose.createConnection("mongodb://localhost/ControlH3");
    autoIncrement.initialize(connection);
var scheduleSchema = new Schema(schema);

scheduleSchema.plugin(autoIncrement.plugin, 'schedule');
module.exports = mongoose.model('schedule', scheduleSchema);